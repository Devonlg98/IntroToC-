#pragma once
#include <iostream>
#include <cmath>
#ifndef  _UTILS_H_
#define _UTILS_H_
void printNumbers(int[], size_t);
int sumNumbers(int[], size_t);
int largestValue(int [], size_t);
int findIndex(int [], size_t, int, int);
int countElement(int [], size_t , int, int);
bool arrayUniqueness(int[], size_t);
void reverseArray(int [], size_t);
void getAllUniqueElements(double [], size_t);


#endif // ! _UTILS_H_